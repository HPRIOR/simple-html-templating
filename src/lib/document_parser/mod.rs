use std::path::PathBuf;

use crate::document_parser::html_fragment::HtmlBody;
mod html_fragment;


pub fn parse_documents(documents: &Vec<String>) -> Vec<HtmlBody> {
    // object with raw text
    //documents.into_iter().map(|text| HtmlBody::new()).collect()
    panic!("");
}
